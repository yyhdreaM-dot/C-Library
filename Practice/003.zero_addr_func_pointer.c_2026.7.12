#include <stdio.h>
int main()
{
   (*(void (*)())0)();
   //调用0地址处的函数
   //该函数无参 返回类型是void
   //1.void(*)() 是函数指针类型
   //2.(void(*)())0 对0进行强制类型转换，被解释为一个函数地址
   //3.*(void(*)())0 对0地址进行解引用
   //4.(*(void(*)())0) 调用0地址处的函数
   return 0;
}
